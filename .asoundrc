# Minimal working ALSA configuration for SH-04 USB microphone
# Direct hardware access without plugin conflicts

# Default PCM device - direct hardware access to SH-04
pcm.!default {
    type hw
    card 2
    device 0
}

# Default control device
ctl.!default {
    type hw
    card 2
}


