# Minimal ALSA configuration for USB microphone in Docker
# Direct hardware access only - no plugins

# Default PCM device - direct hardware access
pcm.!default {
    type hw
    card 0
    device 0
}

# Default control device
ctl.!default {
    type hw
    card 0
}


