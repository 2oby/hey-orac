# Minimal ALSA configuration for USB microphone in Docker
# Only configure what we need - don't override system PCMs

# Default PCM device - direct hardware access to SH-04
pcm.!default {
    type hw
    card SH04
    device 0
}

# Default control device
ctl.!default {
    type hw
    card SH04
}


